version: "3.9"

services:
  api:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: >
      sh -c "pip install -r requirements.txt &&
             python -m app.main"
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=info

